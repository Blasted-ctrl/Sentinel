.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.git/
tests/
# Datasets + training-only artifacts (not needed to serve).
artifacts/
data/
# The serving weights under metrics/ (committed via Git LFS) ARE baked in so the
# image is self-contained; only the large training-only npz is kept out.
metrics/lstm/ensemble_inputs.npz
